# Deployment

Run `python scripts/preflight.py` and `python -m pytest tests/direct -q` from this directory. The deployable source is `contracts/signalbond.py`; record finalized deployment, commit, and source SHA-256, then compare retrieved Explorer source byte-for-byte. No live deployment is claimed by this initial package.
